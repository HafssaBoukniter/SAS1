const prompt = require('prompt-sync')();
/*
//challenge 1
function additioner(a,b){
    return a + b;
}

let result = additioner(5,7);
console.log(`La somme est : ${result}`);

//challenge 2
const multiplier = (x,y) => x * y;

let x = 4;
let y = 5;

console.log(`Le produit de ${x} par ${y} donne : ${multiplier(x, y)}`);

//challenge 3
function maximum(a,b){
    if(a > b){
        return a;
    }else{
        return b;
    }
}

console.log(`Le plus grand nombres est ${maximum(5,7)}`);

//challenge 4
function manimum(a,b){
    if(a < b){
        return a;
    }else{
        return b;
    }
}

console.log(`Le plus petit nombres est ${manimum(5,7)}`);

//challenge 5
function factorielle(number){
    let factorielle = 1;
    for (let i = 1; i <= number; i++) {
        factorielle *= i;
    }
    return factorielle;
}
let number = parseInt(prompt("Saisir un nombre positive :"));
console.log(`La factorielle d'un ${number} est ${factorielle(number)}`);

//challenge 6
function fibonacci(num){
    if(num <= 0){
        return 0;
    }else if(num == 1){
        return 1;
    }else{
        return fibonacci(num - 1) + fibonacci(num - 2);
    }
}
let num = parseInt(prompt("Saisir un nombre :"));
let resultat = 0;
for(let i = 0;i < num;i++){
    resultat = fibonacci(num);
}
console.log(`La fibonacci de ${num} est : ${resultat}`);
*/
//challenge 7

/*
//challenge 8
function paireImpaire(num1){
    if(num1 % 2 == 0){
        return true;
    }else{
        return false;
    }
}
let num1 = parseInt(prompt("Saisir un nombre entier :"));
if(paireImpaire(num1)){
    console.log(`Le nombre ${num1} est paire`);
}else{
    console.log(`Le nombre ${num1} est impaire`);
}
*/